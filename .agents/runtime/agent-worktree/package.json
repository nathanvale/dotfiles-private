{
  "name": "agent-worktree",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-native worktree lifecycle and recovery runtime.",
  "exports": {
    ".": "./src/index.ts",
    "./cli": "./src/cli.ts"
  },
  "scripts": {
    "agent-worktree": "bun run src/cli.ts",
    "lint": "biome check --diagnostic-level=error .",
    "test": "bun test ./tests/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@side-quest/cli-command-facade": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
