{
  "name": "@side-quest/browser-use-security",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser Use Security native product owner: per-target identity, admission manifest, and drift proof for the three signed targets (ADR 0027/0028).",
  "exports": {
    ".": "./src/index.ts",
    "./cli": "./src/cli.ts"
  },
  "scripts": {
    "build:unsigned": "xcodebuild -project BrowserUseSecurity.xcodeproj -scheme ApprovalBroker -configuration Debug -destination 'platform=macOS' -derivedDataPath .build/xcode CODE_SIGNING_ALLOWED=NO build",
    "install:binding-selection-local": "bun run scripts/install-binding-selection-capability.ts",
    "test": "bun test ./tests/*.test.ts",
    "test:protocol": "xcodebuild -project BrowserUseSecurity.xcodeproj -scheme ApprovalBroker -configuration Debug -destination 'platform=macOS' -derivedDataPath .build/xcode CODE_SIGNING_ALLOWED=NO test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "6.0.3"
  }
}
