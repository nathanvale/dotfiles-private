# Bat Configuration
# Only show numbers/headers for file viewing, not for piped input

# Default style (used when viewing files)
--style="numbers,changes,header"

# Theme
--theme="Night Owl"

# Italic text
--italic-text=always

# Paging
--paging=always
