// The one hosted endpoint the Firecrawl account Provider may send its key to.
// Routine tests replace this leaf only inside a copied plugin root, so the
// shipped tree can reach no other origin with the key.
export const ACCOUNT_ENDPOINT = "https://mcp.firecrawl.dev/v2/mcp";
