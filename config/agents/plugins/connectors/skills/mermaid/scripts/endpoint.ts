// The one hosted endpoint the Mermaid account Provider may send its token
// to. Routine tests replace this leaf only inside a copied plugin root, so
// the shipped tree can reach no other origin with the credential.
export const ACCOUNT_ENDPOINT = "https://mcp.mermaid.ai/mcp";
