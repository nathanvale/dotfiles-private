// The one hosted endpoint the Context7 account Provider may send its key to.
// Routine tests replace this leaf only inside a copied plugin root, so the
// shipped tree can reach no other origin with the key.
export const ACCOUNT_ENDPOINT = "https://mcp.context7.com/mcp";
