interface:
  display_name: "Coordinator Role Startup"
  short_description: "Start up as a Herdr project's Stage Manager coordinator"
  default_prompt: "Use $stage-manager to start up as this project's coordinator."
policy:
  allow_implicit_invocation: false
