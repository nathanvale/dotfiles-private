interface:
  display_name: "Stage Manager role"
  short_description: "Take the coordinator role for a Herdr project"
  default_prompt: "Use $stage-manager to start up as this project's coordinator."
policy:
  allow_implicit_invocation: false
