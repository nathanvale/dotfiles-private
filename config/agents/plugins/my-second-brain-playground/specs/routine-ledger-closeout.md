# Routine Ledger closeout

## Outcome and boundary

An ordinary existing-Task note request uses the installed routine route to start
its actual execution and reconcile completion before returning. The former early
exit bypassed both tracking pointers. Add those pointers to the ordered routine
route; retain the shared Ledger Steward branch as the transition owner.

Task: `prove-routine-slice-closeout-keeps-agent-ledger-synchronized-wit-1dd116c24ea0`.
Register: `/Users/nathanvale/.local/share/my-second-brain-playground/registers/playground.sqlite3`.

Preserve independent acceptance, execution identity and completion generation.
Pending evidence leaves work open. A later verdict resumes the same closeout
without a user reminder. Simple transitions stay inline. Do not add hooks,
runtime APIs, roles, tracking stores or product features.

## Qualification

Use one fresh ordinary participant with the installed workflow in the existing
synthetic Maple project, whose first goal is unused. Give a bounded note request, accepted Task and
normal entry. Observe start, note edit and pending acceptance. An independent
reader judges the full accepted checks; return that verdict as ordinary work
continuation and observe completion without a separate tracking instruction.
Retain the original prompt and run. Compare files and events before and after,
including unrelated Tasks and README files. Reuse the Ledger Steward trial for
its existing incomplete-evidence and routing proof; do not repeat it.

Private evidence owner:
`~/.local/state/my-second-brain-playground/prerequisite-implementation-20260905/`.
Raw receipts have 30-day retention; Nathan owns deletion. The existing project
avoids overwriting uncommitted goals or creating another project packet. The
checkpoint selects its vault through user configuration, so an unconfigured
disposable copy would exercise a different boundary.
Stop on lost ownership or unsafe effects. Missing acceptance remains incomplete.

## Retained failures and checkpoint repair

The Maple run completed its accepted note and matching Ledger execution after
independent acceptance. It cannot establish uncoached behavior because its normal
evidence pointer exposed evaluator material. Its first handback took 412.637
seconds. The corrected Lantern run isolated evaluator material, but its native
worker inherited the parent's session ID and implicit bind replaced the parent's
checkpoint. That trial stopped with its business note accepted and lifecycle open.

The repair stays in the existing checkpoint process: implicit bind creates absent
state or refreshes the same work identity; another or unreadable saved identity
is a nonretryable ownership refusal that preserves checkpoint bytes. A private
session lock covers the guard and publication for both write routes. Lock
contention refuses before publication and provides a retry route. The existing
full-input write remains the intentional replacement surface after caller
ownership verification. It does not establish native-worker session delivery.

Native workers compare their own thread identity with the exposed session before
binding; an inherited ID leaves checkpoint recovery unavailable. The routine
still performs independently authorized note and Task work from the supplied goal.
Both failed trials remain failed. The repair's process proof does not substitute
for a new ordinary end-to-end qualification.
