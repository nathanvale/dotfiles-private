#!/bin/sh
# Fixture bd for tests and the cli-design checker: replays recorded output of the pinned bd (1.3.0), writes nothing.
set -eu
here=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
exec bun "$here/bd-shim.ts" "$@"
