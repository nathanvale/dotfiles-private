## Debugging Workflow

When a test, build, or command fails:

- Blocked after 2 attempts with different approaches: stop and explain what you tried.
- Never add `try/catch` or `|| true` to hide a failure you do not understand.
