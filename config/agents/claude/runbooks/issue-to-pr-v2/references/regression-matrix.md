# Issue-to-PR V2 Regression Matrix

Historical behavior-preservation map for the v1 → v2 refactor (issue #48, U1).

This file is the **single writable target** of the `v2-contract-coverage` seam
in `docs/runbooks/issue-to-pr-v2-refactor/`. It maps historical v1 sections and
helper command groups to their current v2-owned homes, then records preservation
evidence for the U1 prose-only invariants and deterministic probe targets.

Historical source paths in this matrix are provenance coordinates, not live
filesystem dependencies. Current contract owners are the v2 destinations and
test anchors.

Authoritative seam: [`docs/runbooks/issue-to-pr-v2-refactor/v2-contract-coverage.md`](../../../docs/runbooks/issue-to-pr-v2-refactor/v2-contract-coverage.md).

Source plan: [`docs/plans/2026-05-22-002-refactor-issue-to-pr-v2-runbook-plan.md`](../../../docs/plans/2026-05-22-002-refactor-issue-to-pr-v2-runbook-plan.md), Unit U1.

## How rows work

| Field | Tables | Meaning |
| --- | --- | --- |
| `id` | all | Stable kebab-case identifier. Used as the dedupe signature for findings against this matrix. |
| `invariant` / `probe target` | prose-only invariants (`invariant`); probe targets (`probe target`); Out-of-scope rows (`invariant`); Removed-by-design rows (`invariant`) | The v1 prose-only invariant or deterministic probe target being mapped. Each sub-table's header uses the specific name shown in parentheses. |
| `v1 source` | all | Historical source coordinate from the refactor baseline. Do not resolve on disk. |
| `current contract` | line-map | One-sentence summary of the v1 contract the row maps (what the current major section, helper command group, or carved-out region of v1 actually says). |
| `v2 destination(s)` | line-map; prose-only invariants; probe targets | Where the v1 behavior lands in v2: a reference path, template path, `cli.ts` command family, or explicit `removed-by-design: <reason>`. A row may list more than one destination when a single coherent v1 unit splits cleanly across v2 destinations (parse vs. emit; Builder rules vs. loop structure); each named destination is qualified by the slice of v1 content it owns. The Out-of-scope rows table uses `future seam` instead of `v2 destination(s)` because U1 does not own the mapping. |
| `v1 scenario` | prose-only invariants; probe targets | One-sentence description of the v1 behavior that proves the invariant exists. |
| `v2 scenario` | prose-only invariants; probe targets | One-sentence description of the v2 behavior that proves the invariant survived. |
| `probe` | prose-only invariants; probe targets | Manual or automated check that verifies the v2 scenario. `manual` rows go in the manual regression matrix; named `cli.ts ... --json` rows become automated probes in U9. |
| `owner unit` | line-map; prose-only invariants; probe targets | Which v2 implementation unit owns landing the destination (U2 references/templates, U3 modules, U4 CLI, U5 packets, U6 versioning, U7 router, U8 README, U9 probes). The Out-of-scope rows table omits this field because U1 does not own the mapping; the future-seam citation in that table names the unit instead. |
| `status` | line-map; prose-only invariants; probe targets | `mapped` (destination confirmed) / `unmapped` (no v2 home yet) / `removed` (intentionally dropped). The Out-of-scope rows table omits `status` because every row in that table is implicitly `closed-not-in-u1-scope` and uses `out-of-scope reason` plus `future seam` columns instead. The Removed-by-design rows table is currently empty; when populated, rows will name the v1 source, removal reason, and rationale location instead of a status. |

## Current contract line-map (U1 scope)

Every current major section and helper command group has one or more v2
destinations (or an explicit removal reason) before extraction begins. A row
with multiple destinations qualifies each destination by the slice of v1
content it owns; see the line-range convention below.

**Line-range convention:** ranges name the section's content boundaries; a row
may include the trailing blank line before the next heading (e.g. `L466-513`
covers Stage 2 prose ending at L512 plus the L513 blank line). **Each
substantive content line belongs to exactly one row.** Structural blank lines
and `---` separators between sections are unassigned by design — they carry
no contract and may sit between adjacent row ranges. Rows may pin discrete
spans (`L1-29, L146-159, L191-238, L415-459`) when a single conceptual
destination spans non-contiguous regions. A row may declare more than one v2
destination when a single coherent v1 unit splits cleanly across v2
destinations (parse vs. emit; Builder rules vs. loop structure); each named
destination must be qualified by the slice of v1 content it owns.

| id | v1 source | current contract | v2 destination(s) | owner unit | status |
| --- | --- | --- | --- | --- | --- |
| issue-to-pr-metadata-and-invocation | `runbooks/issue-to-pr/issue-to-pr.md` L1-11 | Runbook purpose, per-issue ledger location, invocation pointer, and turn-protocol pointer. | `runbooks/issue-to-pr-v2/issue-to-pr.md` hot-router header | U7 | mapped |
| issue-to-pr-file-scope | `runbooks/issue-to-pr/issue-to-pr.md` L13-20 | Builder edit authority is bounded to current batch files; out-of-scope edits fail-stop. | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | U2 | mapped |
| issue-to-pr-reviewers-and-adr-guardrails | `runbooks/issue-to-pr/issue-to-pr.md` L22-41 | Always-on reviewer set, conditional reviewer pointer, and ADR guardrails. | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | U2 | mapped |
| issue-to-pr-role-boundaries | `runbooks/issue-to-pr/issue-to-pr.md` L43-65 | Orchestrator owns routing and gates; Builder implements one confirmed batch; Validator stays read-only. | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | U2 | mapped |
| issue-to-pr-builder-dispatch-contract | `runbooks/issue-to-pr/issue-to-pr.md` L67-265 | Builder packet boundaries, authority and local law, preflight, allowed probes, return envelope, and replacement-batch mechanics (range extended to L265 to capture the final sentence `helper validation rejects the duplicate dependency before confirmation.`). | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | U2 | mapped |
| issue-to-pr-scoped-audit-prompt | `runbooks/issue-to-pr/issue-to-pr.md` L267-272 | The current hot file says final-review uses ce-code-review's own prompt rather than declaring one locally; v2 keeps the no-local-prompt rule but loads the review reference list explicitly. | `runbooks/issue-to-pr-v2/references/stage-5-final-review.md` (final-review reference list); ce-code-review owns the audit prompt itself | U2 | mapped |
| issue-to-pr-stage-preconditions-and-routing | `runbooks/issue-to-pr/issue-to-pr.md` L295-305, L314-337 | `## Stages` header, six-stages-in-order framing, the `decompose.ts --confirmation-state` helper invocation, digest recomputation prose, and helper execution context. (The clean-tree precondition at L276-291 and the resumed-routing rule plus state-meaning catalog at L306-313 are tracked in the Out-of-scope rows section as `clean-tree-precondition` and `confirmation-state-routing` to avoid collision; the `---` separator at L293 is unassigned by the convention.) | `runbooks/issue-to-pr-v2/references/ledger-and-helper.md` | U2 | mapped |
| issue-to-pr-stage-1-pick-issue | `runbooks/issue-to-pr/issue-to-pr.md` L339-464 | Issue inspection, blocker handling, branch preflight, AC extraction and confirmation, ledger creation. | `runbooks/issue-to-pr-v2/references/stage-1-pick-issue.md` | U2 | mapped |
| issue-to-pr-stage-2-plan | `runbooks/issue-to-pr/issue-to-pr.md` L466-513 | ce-plan invocation, plan review, and plan-path checkpointing. | `runbooks/issue-to-pr-v2/references/stage-2-plan.md` | U2 | mapped |
| issue-to-pr-stage-3-decompose | `runbooks/issue-to-pr/issue-to-pr.md` L514-681 | Batch contract generation, AC coverage, contract review, digest confirmation, and stale-contract routing. | `runbooks/issue-to-pr-v2/references/stage-3-decompose.md` | U2 | mapped |
| issue-to-pr-stage-4-batch-loop | `runbooks/issue-to-pr/issue-to-pr.md` L683-753 | Eligible batch selection, host readiness, lifecycle checkpoints, convergence, accepted risk, and batch-loop exit. | `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` (batch selection, lifecycle checkpoints, convergence, accepted risk, batch-loop exit at L683-701 and L715-753) and `runbooks/issue-to-pr-v2/references/host-adapters.md` (pre-implementation host-readiness check at L742-757, per the `host-readiness-vs-infra-failure` invariant) | U2 | mapped |
| issue-to-pr-stage-5-final-review | `runbooks/issue-to-pr/issue-to-pr.md` L755-901 | Read-only final review, final findings gate, patch-batch proposal path, and final P2/P3 closure. (The Mechanical-diff fallback at L784-793 is owned by the `mechanical-diff-fallback` prose-only invariant row and routed to `findings-and-validators.md` to avoid collision; structural blanks at L783, L794, L887 bridge slices and are unassigned by convention.) | `runbooks/issue-to-pr-v2/references/stage-5-final-review.md` (read-only final-review gate at L755-782 and L795-805; final P2/P3 closure at L888-901), `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` (patch-batch decision tree and smallest-contract-patch heuristic at L806-886, per the U2 approach of moving patch-batch remediation into the batch-loop reference; matches the `final-review-patch-decision-tree` and `smallest-contract-patch-heuristic` invariants), and `runbooks/issue-to-pr-v2/references/findings-and-validators.md` (Mechanical-diff fallback at L784-793, per the `mechanical-diff-fallback` invariant) | U2 | mapped |
| issue-to-pr-stage-6-ship | `runbooks/issue-to-pr/issue-to-pr.md` L903-978 | Final checks, residual finding gate, PR creation, PR URL recording, and shipped ledger checkpoint. | `runbooks/issue-to-pr-v2/references/stage-6-ship.md` | U2 | mapped |
| issue-to-pr-personas-and-findings | `runbooks/issue-to-pr/issue-to-pr.md` L982-1016, L1110-1195 | Persona selector table, broad-reviewer fallback, Validator invocation rules, and finding persistence and lifecycle. (Inner-loop escape hatches at L1196-1211 are tracked in the Out-of-scope rows section as `escape-hatches` to avoid collision.) | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | U2 | mapped |
| issue-to-pr-inner-loop-and-builder-rules | `runbooks/issue-to-pr/issue-to-pr.md` L1017-1035, L1038-1084 | Inner-loop diagram, host-readiness vs infrastructure-failure post-dispatch behavior, and the seven Builder execution rules applied every iteration. (The iteration cap at L1086-1088 is mapped by the `stage-4-total-implementation-attempt-cap` U7 row below to avoid collision; structural blanks at L1038, L1045, L1055 bridge slices and are unassigned by convention.) | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` (Builder rules at L1056-1109), `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` (loop diagram at L1017-1035), and `runbooks/issue-to-pr-v2/references/host-adapters.md` (pre-implementation readiness repeat at L1090-1099 and post-dispatch `builder-infrastructure-failure` rule at L1101-1109, per the `host-readiness-vs-infra-failure` invariant) | U2 | mapped |
| issue-to-pr-ce-plan-addendum | `runbooks/issue-to-pr/issue-to-pr.md` L1213-1286 | ce-plan addendum and structured-output requirements for batch contracts. | `runbooks/issue-to-pr-v2/templates/ce-plan-addendum.md` | U2 | mapped |
| issue-to-pr-close-and-loop-fallback | `runbooks/issue-to-pr/issue-to-pr.md` L1288-1313 | Finding close reasons and loop fallback invocation (range includes the closing fence of the `/loop` code block at L1313). | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | U2 | mapped |
| readme-invocation-driver-and-index | `runbooks/issue-to-pr/README.md` L1-29, L140-159, L191-238, L415-459 | Human-facing purpose, "Why these seams" map, host-neutral invocation prose, `/goal` block, driver compatibility, execution order, closing reports, parallel execution, and "What this area deliberately does not do" exclusions. | `runbooks/issue-to-pr-v2/README.md` human index | U8 | mapped |
| readme-compatibility-and-builder-overview | `runbooks/issue-to-pr/README.md` L30-139 | Issue shape compatibility, execution mode ownership, Builder dispatch overview, and patch proposal summary. | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | U2 | mapped |
| readme-helper-ledger-and-turn-protocol | `runbooks/issue-to-pr/README.md` L161-175, L239-258, L348-414 | Helper execution context, shared turn protocol, and ledger schema overview. (The `### Confirmation State` section at L176-189 is tracked in the Out-of-scope rows section as `confirmation-state-routing` to avoid collision.) | `runbooks/issue-to-pr-v2/references/ledger-and-helper.md` | U2 | mapped |
| readme-fix-risk-and-glossary | `runbooks/issue-to-pr/README.md` L260-346 | Fix protocol, risk classification, and local glossary definitions (range extended to L346 to capture the second line of the `Route hint` glossary entry). | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | U2 | mapped |
| ledger-frontmatter-template | `runbooks/issue-to-pr/issue-N-ledger.template.md` L1-21 | Per-issue ledger frontmatter schema. | `runbooks/issue-to-pr-v2/cli.ts ledger-init --json` | U6 | mapped |
| ledger-template-title-and-pointer | `runbooks/issue-to-pr/issue-N-ledger.template.md` L22-27 | Per-issue ledger title placeholder (`# Issue {issue_number} - {issue_title}`) plus prose pointer back to the workflow runbook and the README for format/protocol. | `runbooks/issue-to-pr-v2/cli.ts ledger-init --json` (title and pointer render in `ledger_markdown`) | U6 | mapped |
| ledger-acceptance-and-batches | `runbooks/issue-to-pr/issue-N-ledger.template.md` L28-61 | Acceptance criteria section and batches YAML contract. | `runbooks/issue-to-pr-v2/references/ledger-and-helper.md` | U2 | mapped |
| ledger-findings-and-notes | `runbooks/issue-to-pr/issue-N-ledger.template.md` L63-93 | Findings data, rendered findings table, and notes evidence log. | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | U2 | mapped |
| helper-digest-command-group | `runbooks/issue-to-pr/decompose.ts` L2109-2118, L2129-2133, L2171-2172 | `--plan-digest`, `--ac-digest`, `--batch-contract-digest`, and `--candidate-contract-digest` command behavior (digest emitters and the candidate-contract digest emit on the default path). | `runbooks/issue-to-pr-v2/lib/digest.ts` | U3 | mapped |
| helper-confirmation-state-mode | `runbooks/issue-to-pr/decompose.ts` L2119-2123 | `--confirmation-state` line-oriented confirmation-state behavior. | `runbooks/issue-to-pr-v2/cli.ts state --json` | U4 | mapped |
| helper-batch-and-ac-validation-group | `runbooks/issue-to-pr/decompose.ts` L2124-2128, L2173-2174 | `--validate-ledger-batches` and `--validate-ac-coverage` behavior (the dispatch arm and the validation emit on the default path). | `runbooks/issue-to-pr-v2/lib/validate.ts` | U3 | mapped |
| helper-findings-gate-group | `runbooks/issue-to-pr/decompose.ts` L2134-2143 | `--validate-findings` and `--assert-no-open-p0p1` behavior. | `runbooks/issue-to-pr-v2/lib/validate.ts` | U3 | mapped |
| helper-patch-proposal-mode | `runbooks/issue-to-pr/decompose.ts` L2161-2163 | Patch-proposal-specific `readLedgerBatchContext(...)` wiring that feeds `parse(...)` with the existing ledger batch context for proposer-only DAG validation. | `runbooks/issue-to-pr-v2/lib/validate.ts` | U3 | mapped |
| helper-compat-entrypoint-and-usage | `runbooks/issue-to-pr/decompose.ts` L2108, L2144-2160 | Argument slice, mode-flag detection (`patchProposalMode`, `validateCoverage`, `candidateContractDigest`), dispatcher usage-string, and `planPath/validateFlag/ledgerPath` arg unwrap shared across all dispatch arms. | `runbooks/issue-to-pr-v2/decompose.ts` compatibility shim | U3 | mapped |
| helper-decompose-default-mode | `runbooks/issue-to-pr/decompose.ts` L2164-2170, L2175-2177 | Default flag-less `decompose.ts <plan-path>` parse-and-emit behavior: `parse(planPath, { ... })` reads the plan with optional ledger batch context, then `emit(batches)` writes batches JSON on the default branch. | `runbooks/issue-to-pr-v2/lib/validate.ts` (`parse(...)` consumes the contract spec) and `runbooks/issue-to-pr-v2/decompose.ts` compatibility shim (`emit(...)` retains v1 stdout shape) | U3 | mapped |

## Prose-only invariants (U1 + U7 scope)

The `test_anchor` column is U9's audit-trail addition. Rows marked
`manual` are judgment-heavy (Builder authority, persona dispatch,
mechanic discipline, inline eligibility, or temporal sequencing) and an
automated probe cannot exercise them without modeling the runtime
judgment. The column is filled for symmetry with the deterministic-probe
table below; each value names *why* the row stays manual, or names the
partial helper coverage when durable ledger evidence is machine-checked.

| id | invariant | v1 source | v2 destination(s) | v1 scenario | v2 scenario | probe | owner unit | status | test_anchor |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| local-law-read-order | Local Law Read Order | `runbooks/issue-to-pr/issue-to-pr.md` L118-131 ("### Authority and Local Law") | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | Builder reads target-repo AGENTS.md, package AGENTS.md, package CONTEXT.md, referenced governance docs, `batch.files`, then nearby tests, in that order before any edit. | Builder dispatch reference loads the same six-step read order under a "Local Law Read Order" heading; Builder template links to it; hot router cites it from one place. | manual | U2 | mapped | `manual: read-order compliance is observable only when Builder runs against a target repo; no static test can simulate the six AGENTS.md / CONTEXT.md / batch.files / nearby-tests trace.` |
| mechanic-discipline | Mechanic Discipline | `runbooks/issue-to-pr/issue-to-pr.md` L133-138 (canonical rule body; the two-line README glossary pointer at L343-344 stays with the `readme-fix-risk-and-glossary` line-map row routed to `findings-and-validators.md` as a definitional cross-reference rather than the rule itself) | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | Builder finds existing seam, makes smallest coherent diff, avoids opportunistic cleanup/speculative abstractions/dependency changes, preserves local language, runs targeted checks, reports uncertainty. | Builder dispatch reference enumerates the same seven discipline rules; Builder Work Packet template references them; v2 router doesn't restate them inline; the v2 findings-and-validators glossary keeps a pointer to the canonical rule. | manual | U2 | mapped | `manual: discipline rules (smallest coherent diff, no opportunistic cleanup) are judgment calls Validators evaluate per-diff; no static probe substitutes for reviewer judgment.` |
| public-contract-rule | Public Contract Rule | `runbooks/issue-to-pr/issue-to-pr.md` L139-144 | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | Builder may change exported symbols, API shapes, CLI flags, schemas, event payloads, config, env vars, migrations, or package boundaries only when the confirmed batch contract names that surface and includes checks. | Builder dispatch reference owns the rule under a "Public Contract Rule" heading; helper validation still rejects unauthorized public surface changes via batch contract digest. | manual | U2 | mapped | `manual: enforcement is a Validator persona judgment against the diff; the batch_contract_digest helper catches mutations of the contract itself but not violations within it.` |
| domain-language-rule | Domain Language Rule | `runbooks/issue-to-pr/issue-to-pr.md` L145-149 | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | Builder preserves existing target-repo language; unowned terms only in envelope, provisionally; fail-stops if missing language affects ownership/API/behavior/durable meaning. | Builder dispatch reference owns the rule; Builder template's `<local_law_read_order>` framing prompts the language preservation behavior; v2 router cites once. | manual | U2 | mapped | `manual: linguistic preservation is a per-repo judgment; no static probe captures "the project calls this X, not Y".` |
| preflight-checklist | Builder Preflight Checklist | `runbooks/issue-to-pr/issue-to-pr.md` L150-167 ("### Builder Preflight Checklist") | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | Builder verifies nine readiness items (task understood, AC present, package ownership clear, seam found, test strategy clear, public API impact known, domain language safe, fixtures available, checks runnable); "no readiness, no build". | Builder dispatch reference owns the nine-item checklist; Builder Work Packet template includes a `<preflight_checklist>` section; Builder fail-stop envelope schema enforces the rule via `fail-stop-preflight` status. | manual | U2 | mapped | `manual: checklist completion is per-Builder-dispatch evidence in the return envelope; helper validation of envelope shape is partial coverage but cannot assert "task was actually understood".` |
| probe-catalog | Probe Catalog | `runbooks/issue-to-pr/issue-to-pr.md` L168-178 | `runbooks/issue-to-pr-v2/references/builder-dispatch.md` | Builder may run only the five named probes (rename path, identity flip, command/path reference, public API, package governance) plus literal probes named by the batch; matches outside `batch.files` must not expand scope. | Builder dispatch reference owns the catalog; Builder template includes the catalog as `<allowed_probes>`; relevant findings outside `batch.files` route to `fail-stop-preflight`. | manual | U2 | mapped | `manual: probe-vs-edit boundary is observed in the Builder return envelope diff scope; the persistence-only helper rule lives in builder_attempts validation but does not enumerate which probes ran.` |
| final-review-patch-decision-tree | Final-review patch decision tree | `runbooks/issue-to-pr/issue-to-pr.md` L806-886 (Stage 5 inner loop) | `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` (per U2 approach: "Move final-review patch-batch remediation into stage-4-batch-loop.md; keep stage-5-final-review.md as a read-only review gate") | If finding fixable in ≤2 files within confirmed batch scope → proposal-only Builder dispatch for one patch-batch; if >2 files → first try smaller contract patch with `contract-softening-exception:` rationale; otherwise fail-stop for replan. | Stage 4 batch-loop reference owns the decision tree; stage-5-final-review.md routes >2-file findings back to it; Proposer envelope template enforces read-only authority. | manual | U2 | mapped | `manual: the ≤2-files / smaller-contract / replan decision is per-finding orchestrator judgment; helper validation enforces the 2-file ceiling on patch-proposal batches but not which branch the orchestrator chose.` |
| smallest-contract-patch-heuristic | Smallest contract patch heuristic | `runbooks/issue-to-pr/issue-to-pr.md` L872-879 | `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` | When a finding's fix would touch >2 files, first ask whether a smaller patch exists that adjusts the cited contract (doc claim, AC, comment, runbook section) rather than full implementation; propose with `contract-softening-exception:` rationale. | Stage 4 batch-loop reference owns the heuristic; helper validation accepts the `contract-softening-exception:` field in patch-batch rationale. | manual | U2 | mapped | `manual: "is there a smaller contract patch" is a judgment about which artifact owns the assertion; no static probe substitutes for that judgment.` |
| mechanical-diff-fallback | Mechanical-diff fallback | `runbooks/issue-to-pr/issue-to-pr.md` L784-793 | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | When >80% of cumulative diff lines are mechanical (renames, identifier substitutions, doc-pointer refreshes), orchestrator may substitute a single `ce-correctness-reviewer` subagent for the full /ce-code-review wave; subagent must include all ledger-recorded signatures for dedupe. | findings-and-validators.md reference owns the rule; orchestrator records the choice and >80% estimate in Notes; signature-dedup rule enforced by helper validation of `## Findings data`. | manual | U2 | mapped | `manual: the 80%-mechanical estimate is operator judgment recorded in Notes; helper validation of finding signatures is partial (it enforces dedup, not the threshold).` |
| broad-reviewer-fallback | Broad-reviewer fallback | `runbooks/issue-to-pr/issue-to-pr.md` L982-1001 ("## Persona selector" / "default broad reviewer set") | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | When path/name signals or Builder focus are incomplete, dispatch the always-on reviewer list plus every conditional reviewer in the table (except ce-previous-comments-reviewer unless PR/issue signal present). | findings-and-validators.md reference owns the persona selection table and the broad-reviewer-fallback condition; Validator envelope template assumes the resolved persona set. | manual | U2 | mapped | `manual: "signals are incomplete" is operator judgment about the diff and the issue; no static probe enumerates "what counts as a signal".` |
| selector-precedence | Selector precedence | `runbooks/issue-to-pr/issue-to-pr.md` L990-996 | `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | Existing path, touched-file, and batch-contract signals must dispatch their validators regardless of Builder suggestion; orchestrator stops only for Builder authority breaches or malformed envelopes; correctness concerns become transient Validator focus, not orchestrator-authored findings. | findings-and-validators.md reference owns the precedence rule; Validator envelope schema separates "must-dispatch signals" from "transient focus hints"; orchestrator code paths don't author Validator findings. | manual | U2 | mapped | `manual: precedence is enforced at dispatch time by the orchestrator runbook; the boundary between "must-dispatch" and "hint" is judgment over which validators applied.` |
| host-readiness-vs-infra-failure | Host-readiness vs infrastructure-failure boundary | `runbooks/issue-to-pr/issue-to-pr.md` L742-757 (pre-implementation in Stage 4 batch-loop), L1090-1099 (pre-implementation repeat inside `## Inner loop`, `host-builder-tools-unavailable`), L1101-1109 (post-dispatch `builder-infrastructure-failure`) — canonical rule bodies; the README outcome summary at L164-174 and glossary entries at L378-384 stay with the `readme-helper-ledger-and-turn-protocol` and `readme-fix-risk-and-glossary` line-map rows as cross-references rather than the rule itself | `runbooks/issue-to-pr-v2/references/host-adapters.md` | Pre-implementation capability failure → `blocked_reason: host-builder-tools-unavailable`, no Orchestrator-inline fallback, no implementation attempt evidence, no iteration increment, no Validator dispatch; post-dispatch tool/permission/serialization/envelope failure → `blocked_reason: builder-infrastructure-failure`, batch stays in-progress, no `builder_attempts` row appended, iterations not incremented, Validators not dispatched. | host-adapters.md reference owns the boundary; Builder dispatch contract enumerates the two blocked_reason values; helper validation of frontmatter accepts both strings; the v2 findings-and-validators glossary keeps a pointer to the canonical boundary in host-adapters.md. | manual | U2 | mapped | `manual: the pre-implementation-vs-post-dispatch boundary depends on host runtime state at the moment an implementation attempt would begin or Builder dispatch has begun; no static probe can fabricate "the host had the tools but the dispatch then failed".` |
| stage-4-dispatch-path-selection | Stage 4 dispatch path selection | `runbooks/issue-to-pr/issue-to-pr.md` L683-753, L1017-1109; `docs/adr/0001-stage-4-context-isolation.md`; `docs/adr/0003-stage-4-keeps-always-on-validator-wave.md` | `skills/issue-to-pr/SKILL.md`, `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md`, `runbooks/issue-to-pr-v2/references/builder-dispatch.md`, `runbooks/issue-to-pr-v2/references/findings-and-validators.md` | Stage 4 requires Builder for `tdd`, `proof_first`, repairs after open P0/P1, patch-batches, and `change_first` once a dispatch trigger fires; bounded `change_first` may remain Orchestrator-inline only while the narrow eligibility rule holds. | The active skill and Stage 4 references name Builder and Orchestrator-inline implementation as distinct paths, keep host readiness before either path, and route every open P0/P1 repair back to Builder. | manual | U7 | mapped | `manual: dispatch-trigger judgment depends on touched-file count, risk, behavioural/public-contract/governance surfaces, uncertainty, and Orchestrator context load at runtime; static checks pin the resulting durable evidence, not the operator's path judgment.` |
| stage-4-attempt-checkpoint-and-validator-wave | Attempt checkpoint and completed Validator-wave evidence | `runbooks/issue-to-pr/issue-to-pr.md` L1017-1035; `docs/adr/0003-stage-4-keeps-always-on-validator-wave.md` | `skills/issue-to-pr/SKILL.md`, `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md`, `runbooks/issue-to-pr-v2/references/findings-and-validators.md`, `runbooks/issue-to-pr-v2/references/ledger-and-helper.md` | Every committed implementation attempt must have a ledger-only `implementation_attempt_checkpoint` before Validator packet rendering, then full Validator-wave completion evidence tied to the same commit and attempt lane, including clean `findings: []` waves. | The skill exposes `attempt-checkpoint` and `validator-wave` as separate Stage 4 visible actions; ledger/helper validation rejects terminal committed attempts without matching checkpoint and completed-wave evidence. | `decompose.ts --validate-ledger-batches` plus manual order check | U7 | mapped | `runbooks/issue-to-pr-v2/lib/ledger.test.ts: "accepts a current-version terminal inline attempt with clean completed-wave evidence", "rejects a current-version terminal committed attempt without completed-wave evidence", "rejects a current-version terminal committed attempt without checkpoint evidence", "rejects an inline terminal attempt when checkpoint+wave evidence is wrongly labeled builder_attempts"; manual: final ledger content validates durable relationship, while actual before-packet temporal ordering is an operator sequence requirement.` |
| stage-4-total-implementation-attempt-cap | Total implementation-attempt cap | `runbooks/issue-to-pr/issue-to-pr.md` L1086-1088 | `runbooks/issue-to-pr-v2/references/ledger-and-helper.md`, `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md`, `runbooks/issue-to-pr-v2/lib/ledger.ts`, `runbooks/issue-to-pr-v2/decompose.ts` | The old Builder-envelope cap is now a five implementation-attempt cap across well-formed Builder attempts plus committed Orchestrator-inline attempts; Builder infrastructure failures stay outside both lanes and outside the cap. | Ledger docs and helper validation count both lanes for `iterations`, reject mismatched counts and six total implementation attempts, and keep `builder_commits` Builder-only. | `decompose.ts --validate-ledger-batches` | U7 | mapped | `runbooks/issue-to-pr-v2/decompose.test.ts: "accepts committed orchestrator inline attempts as implementation attempts", "rejects malformed orchestrator inline attempt records"` |

## Deterministic probe targets (U1 + U7 scope)

The `test_anchor` column is U9's audit-trail addition. Each value names
the **test file path + verbatim `it()` description** that exercises the
probe; multiple anchors are listed when more than one test covers the
contract from a different angle. A row whose anchor cannot be wired to
a shipped test is a U9 finding (file as `probe-target-no-test-anchor`
with risk `high`).

| id | probe target | v1 source | v2 destination(s) | v1 scenario | v2 scenario | probe | owner unit | status | test_anchor |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| probe-installed-reference-presence | Installed reference/template presence | v1 has no equivalent — ledger template copy at stage 1 (issue-to-pr.md L433-434) is the closest analog but does not validate the wider reference tree | `runbooks/issue-to-pr-v2/cli.ts diagnose --json` (planned, U4) | v1 has no recursive install-topology check; missing references surface as broken links only when an agent tries to load them. | `cli.ts diagnose --json` reports installed-artifact presence (references/, templates/, cli.ts surface, module tree) and emits structured drift for the hot router to block on. | `cli.ts diagnose --json` | U4 | mapped | `runbooks/issue-to-pr-v2/cli.test.ts: "installed_artifact_presence carries the U6 shape (cli_ts/lib_dir/references/templates + all_present + missing)"; runbooks/issue-to-pr-v2/lib/route.test.ts: "default invocation against the live v2 install reports every root present", "non-existent install path collapses every root to missing", "partial install reports only the missing roots", "empty subdirectory is reported as missing (recursive walk requires at least one file)", "only-empty-nested-directory subtree is missing (no file at any depth)", "regular file at a directory-root path reports that root missing", "directory at the cli_ts path reports cli_ts missing"` |
| probe-runbook-version-mismatch | `runbook_version` mismatch | v1 has no equivalent — `runbooks/issue-to-pr/issue-N-ledger.template.md` frontmatter L1-21 has no `runbook_version` field | `runbooks/issue-to-pr-v2/cli.ts state --json` reads `runbook_version` from ledger frontmatter; U6 adds the field and the fail-closed routing | v1 cannot detect a resumed run against a stale or mismatched workflow contract; resumed agents may silently proceed under v2 semantics on a v1 ledger. | Ledger frontmatter gains `runbook_version: 2`; `cli.ts state --json` reports version skew values `matched`, `missing`, `mismatched`, or `continuation-evidence-present`; missing/old/mismatched without explicit continuation evidence is stop-required. | `cli.ts state --json` | U6 | mapped | `runbooks/issue-to-pr-v2/cli.test.ts: "matched skew surfaces runbook_version + runbook_version_skew on state", "missing skew routes to blocked-runbook-version-skew with stop-required gate", "mismatched skew routes to blocked-runbook-version-skew with stop-required gate", "continuation-evidence-present clears the stop-required gate and lets routing proceed", "diagnose envelope mirrors state on runbook_version + runbook_version_skew", "diagnose envelope surfaces frontmatter.runbook_version stop-required gate on missing skew"; runbooks/issue-to-pr-v2/lib/route.test.ts: "blocked-runbook-version-skew when skew is mismatched", "blocked-runbook-version-skew when skew is missing", "matched skew does NOT block", "U6: missing runbook_version_skew emits frontmatter.runbook_version stop-required field gate", "U6: mismatched runbook_version_skew emits frontmatter.runbook_version stop-required field gate", "U6: continuation-evidence-present does NOT emit the runbook_version stop-required gate", "U6: matched runbook_version_skew does NOT emit the stop-required gate", "U6: null runbook_version_skew (no-ledger case) does NOT emit the stop-required gate"` |
| probe-cli-state | `cli.ts state --json` | `runbooks/issue-to-pr/decompose.ts --confirmation-state <ledger-path>` (issue-to-pr.md L304 helper-command bullet; README L180-182 helper invocation block — the L180-182 invocation block plus the L306-313 routing prose both fall within ranges tracked separately in the `confirmation-state-routing` out-of-scope row, which carries no v2 destination so no destination contradiction is induced) reports `acceptance_criteria`, `batch_contract`, `digests` as pending/confirmed/stale/blocked | `runbooks/issue-to-pr-v2/cli.ts state --json` (planned, U4) supersedes/wraps the v1 helper command | v1 helper command returns line-oriented text under `decompose.ts --confirmation-state`; agents parse the lines as prose. | `cli.ts state --json` emits a stable top-level JSON schema covering confirmation state, digest drift, version skew, current route facts, required references, and blocking gates. | `cli.ts state --json` | U4 | mapped | `runbooks/issue-to-pr-v2/cli.test.ts: "state without --json returns missing-json-flag error envelope", "state reports route_id: no-ledger when the ledger file is missing", "envelope carries the stable success shape (status/run_id/started_at_ms/duration_ms/data)", "data carries confirmation_state, digest_drift, version_skew, route_id, required_reference_ids, blocking_gates", "F002 fix: digest_drift carries the full three-axis shape", "F004 fix: envelope carries schema_version: '1'"` |
| probe-cli-diagnose | `cli.ts diagnose --json` | v1 has no equivalent — `decompose.ts` does not validate installed reference/template/CLI/module trees | `runbooks/issue-to-pr-v2/cli.ts diagnose --json` (planned, U4) | v1 cannot report install-topology drift; missing artifacts surface only when prose tries to load them. | `cli.ts diagnose --json` reports installed artifact presence, reference and template drift, findings-table drift, and version skew as structured JSON. | `cli.ts diagnose --json` | U4 | mapped | `runbooks/issue-to-pr-v2/cli.test.ts: "diagnose without --json returns missing-json-flag error envelope", "data carries inferred_route_id, expected_reference_ids, installed_artifact_presence, drift, version_skew", "F037 hoist: drift carries the full DiagnoseDrift shape (digest_drift four-axis + findings_table_drift: null)", "installed_artifact_presence carries the U6 shape (cli_ts/lib_dir/references/templates + all_present + missing)", "diagnose envelope carries the documented version_skew default"` |
| probe-startup-route | Startup route behavior | `runbooks/issue-to-pr/README.md` L239-258 ("Turn protocol (shared)") steps 1-3; helper command `--confirmation-state` routing language at issue-to-pr.md L306-313 | `runbooks/issue-to-pr-v2/cli.ts state --json` (planned, U4) owns the deterministic route facts at README L239-258; router reference in `runbooks/issue-to-pr-v2/issue-to-pr.md` (planned, U7) owns the prose pointer to the routing language at issue-to-pr.md L306-313 | At turn start, agent re-reads runbook, re-reads ledger, runs `--confirmation-state`, then routes pending/stale/blocked gates before relying on ACs/batches/digests. | Same five-step turn protocol; `cli.ts state --json` provides the route facts deterministically; hot router stays under 500 lines by referring to the CLI output instead of re-deriving from prose. | `cli.ts state --json`, `cli.ts next --json`, `cli.ts contract route_ids --json` (the turn protocol reads all three) | U4 | mapped | `runbooks/issue-to-pr-v2/cli.test.ts: "data contains only route_id and ledger_exists — no imperative fields", "the entire stdout payload contains no imperative verbs (F013: word-boundary regex)", "route_ids slice returns the complete route id catalog", "running state twice on the same ledger yields the same route_id", "route_id is always a member of the documented ROUTE_IDS catalog"` |
| probe-cli-smoke-matrix | Process-boundary CLI contract round-trip | v1 has no equivalent — v1 `decompose.ts` is exercised by `decompose.test.ts` via subprocess spawn but there is no v1 equivalent for the v2 `cli.ts` envelope contract | `runbooks/issue-to-pr-v2/cli-smoke.test.ts` (new, U10) spawns `bun cli.ts ...` as a child process and pins the full HELP_DATA contract surface at the process boundary | v1 has no JSON CLI envelope contract to round-trip; v1 helper output is line-oriented prose. | `cli-smoke.test.ts` pins the documented envelope shape across every command × every documented ledger state × every contract slice × every packet role × every documented error_code. Process exit codes are pinned to match envelope `exit_code` values. | `bun cli.ts ...` subprocess invocation | U10 | mapped | `runbooks/issue-to-pr-v2/cli-smoke.test.ts: "Block 1: Envelope-shape invariants (cross-command)", "Block 2: Every command × --json requirement", "Block 3: state command × ledger states", "Block 4: next command × no-imperative contract (ADR 0002)", "Block 5: contract command × every documented slice", "Block 6: diagnose command × ledger states", "Block 7: packet command × every role × required-flag matrix", "Block 8: Every documented error_code", "Block 9: Stdout/stderr separation", "Block 10: Process exit code alignment with envelope exit_code"` |
| probe-cli-batch-loop-route-smoke | Confirmed not-terminal ledger routes to `batch-loop` | v1 uses `decompose.ts --confirmation-state` plus prose routing to enter Stage 4 after AC, plan, and batch contract confirmation | `runbooks/issue-to-pr-v2/cli-smoke.test.ts` | A resumed agent with confirmed ACs, confirmed batch contract, valid digests, and a non-terminal batch enters Stage 4. | `cli.ts state <ledger> --json` reports `route_id: "batch-loop"`, `has_batches: true`, and `all_batches_terminal: false` at the process boundary. | `cli.ts state --json` | U7 | mapped | `runbooks/issue-to-pr-v2/cli-smoke.test.ts: "confirmed not-terminal ledger reports route_id: batch-loop"` |
| probe-ledger-lifecycle-field-drift | Ledger lifecycle field drift | v1 has no equivalent — v1 did not cross-check executable lifecycle key sets | `runbooks/issue-to-pr-v2/contract-drift.ts`, `runbooks/issue-to-pr-v2/references/ledger-and-helper.md`, `runbooks/issue-to-pr-v2/cli.ts` | Ledger schema slice pointers could disappear from operator docs without a machine check. | `contract-drift.ts` reads live CLI schema slices and reports missing ledger schema slice pointers in `ledger-and-helper.md`. | `bun runbooks/issue-to-pr-v2/contract-drift.ts` | U7 | mapped | `runbooks/issue-to-pr-v2/contract-drift.test.ts: "real ledger/helper reference matches emitted schema facts", "missing ledger schema slice pointer in ledger-and-helper.md is reported as drift", "checkContractDrift orchestrator surfaces ledger schema findings from the helper reference"` |
| probe-validator-evidence-source | Validator packet evidence source | v1 Validator handoff assumed Builder envelope evidence | `runbooks/issue-to-pr-v2/lib/packets.ts`, `runbooks/issue-to-pr-v2/cli.ts`, `runbooks/issue-to-pr-v2/templates/validator-envelope.md` | Validator packets must not fabricate Builder evidence when the committed attempt was Orchestrator-inline. | Builder attempts render Builder rich evidence; Orchestrator-inline attempts render compact inline evidence with implementation commit, touched files, inline-validity note, and optional exception note. | `cli.ts packet validator --json` | U7 | mapped | `runbooks/issue-to-pr-v2/lib/packets.test.ts: "strips Builder fix prose: notes and suggested_scope_changes never flow through", "renders inline attempt evidence without Builder envelope fields"; runbooks/issue-to-pr-v2/cli.test.ts: "validator packet through CLI emits inline evidence source when requested", "validator packet rejects inline-only flags without inline evidence source"` |

## Out-of-scope rows

Rows that earlier sweeps surfaced but that U1 does not own. Recorded here so a
later sweep does not re-surface them as new findings; resolution names the
future seam or unit that owns the mapping.

| id | invariant | v1 source | out-of-scope reason | future seam |
| --- | --- | --- | --- | --- |
| confirmation-state-routing | Resumed agent must route from durable ledger state, not transcript memory (rule + four-state catalog: pending/confirmed/stale/blocked) | `runbooks/issue-to-pr/issue-to-pr.md` L306-313 (sentence opens at L306 `The command reports whether...` and continues through the routing rule and the four-state meanings); `runbooks/issue-to-pr/README.md` L176-189 (full `### Confirmation State` section: heading at L176; resumed-turn helper invocation at L178-182; four-state derivation prose at L184-189) | Load-bearing prose invariant but not in U1's listed scope; U1 covers the `probe-cli-state` probe target (the CLI surface that exposes the state), but the *routing rule itself* is a separate behavioral invariant that belongs to a hot-router-cutover or stage-shell seam. | A future seam covering `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` + the v2 hot router invocation contract (U7 work). |
| clean-tree-precondition | Working tree must be clean at every stage transition; non-empty is a runbook bug requiring fail-stop (includes the Stage 4 lifecycle-checkpoint clarification) | `runbooks/issue-to-pr/issue-to-pr.md` L276-291 (full `## Inter-stage precondition: clean tree` section: heading at L276; clean-tree rule body at L278-283; Stage 4 lifecycle-checkpoint clarification at L285-288; fail-stop enforcement at L290-291; intervening blanks at L277/L284/L289 are structural and unassigned by convention) | Load-bearing prose invariant but not in U1's listed scope; U1 maps Builder/Validator/Proposer authority boundaries, not stage-transition preconditions. | A future seam covering `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` lifecycle + stage shells (U2 + U7 work). |
| escape-hatches | Six inner-loop escape hatches (`ADR-contradicts-<id>`, `public-API-change`, `execution-mode-mismatch`, `same-signature-twice`, `risk-high-finding`, `finding-count-rises`) — early-stop conditions in the batch inner loop | `runbooks/issue-to-pr/issue-to-pr.md` L1196-1211 | Load-bearing inner-loop behavioral rules but not in U1's listed 12 prose-only invariants; same shape as `confirmation-state-routing` and `clean-tree-precondition` — stage-4 behavioral invariants belong to a stage-shell seam, not U1's authority-boundary inventory. | A future seam covering `runbooks/issue-to-pr-v2/references/stage-4-batch-loop.md` (U2 + U7 work). |

## Removed-by-design rows

Invariants the v2 refactor intentionally drops. Each row names the v1 source,
the reason for removal, and where the rationale is recorded (plan, ADR, or
discussion). When populated, rows use the schema:
`| id | invariant | v1 source | removal reason | rationale location |` — `status`
is implicitly `removed`; no `v2 destination(s)` or `owner unit` columns apply.

*(none yet)*
