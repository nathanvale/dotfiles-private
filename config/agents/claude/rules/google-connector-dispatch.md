Calendar, email, or contacts questions route through `gog` per the `productivity-connectors` skill: read `.productivity.yml`, pass `--account` explicitly, ask which account if that file is absent.

Google MCP tools (`gcal_*`, `gmail_*`) are decommissioned. Do not call them.
