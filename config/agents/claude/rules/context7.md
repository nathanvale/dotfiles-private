When working with libraries, frameworks, or APIs, use Context7 to fetch current documentation instead of relying on training data. Cite the version when available.

CLI fallback when the MCP tools are absent or auth is blocked:

1. `npx -y ctx7 library <name> "<question>"`
2. `npx -y ctx7 docs <libraryId> "<question>"`

Name the library ID used.
