{
	"name": "session-recovery-scripts",
	"private": true,
	"type": "module",
	"scripts": {
		"session-recovery": "bun run src/cli.ts",
		"lint": "biome check --diagnostic-level=error .",
		"test": "bun test src/*.test.ts",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"dependencies": {
		"@side-quest/session-corpus": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
