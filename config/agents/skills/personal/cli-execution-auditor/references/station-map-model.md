# Station Map Model Routing

Branch Station model owner: `runtime/cli-command-facade/src/station-map.ts`.

Use this local reference when the auditor skill needs model provenance without
copying runtime contracts into `SKILL.md`.
