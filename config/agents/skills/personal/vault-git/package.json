{
	"name": "vault-git-skill",
	"private": true,
	"type": "module",
	"scripts": {
		"lint": "biome check --diagnostic-level=error .",
		"test": "bun test tests/skill-contract.test.ts",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"typescript": "catalog:"
	}
}
