# Personal instruction pointer

Before any task, read `~/.config/agents/global.md` and follow it. If that file
is missing or unreadable, stop and report its exact path.

Also follow every `AGENTS.md` discovered from the current repository root to
the working directory. Do not treat this global file as a replacement for
project-specific instructions.
